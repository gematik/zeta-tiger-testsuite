tigerProxy:
  proxyId: Cluster
  downloadInitialTrafficFromEndpoints: false
  skipTrafficEndpointSubscription: false
  activateRbelParsingFor:
    - asl
  trafficEndpoints:
    - "http://${zeta_proxy_url}"
